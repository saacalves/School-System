


<div class="content-wrapper">
  <%= render partial: 'shared/admin/page_title', locals: { title: 'Usuários' } %>

  <div class="content">
    <div class="container-fluid">
      <div class="row">
        
        <div class="col-lg-12">
          <div class="card card-primary card-outline">
            <div class="card-header">
              <h5 class="m-0">Detalhes</h5>
            </div>
            <div class="card-body">
              <div>
                <h4>E-mail</h4>
                <%= @user.email %>
              </div>

              <div>
                <h4>Criando em</h4>
                <%= @user.created_at %>
              </div>
              <%= link_to 'Voltar', admin_users_path, class: 'btn btn-primary'%>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
